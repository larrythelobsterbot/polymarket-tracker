# Core dependencies
httpx>=0.27.0
python-dotenv>=1.0.0
pydantic>=2.5.0
pydantic-settings>=2.1.0
apscheduler>=3.10.4
tenacity>=8.2.3

# Web dashboard dependencies
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
jinja2>=3.1.0
python-multipart>=0.0.9

# GUI dependencies (tkinter is built into Python)
# No additional packages needed for basic GUI

# Optional: ML dependencies for insider detection
# Uncomment to enable ML-based detection
# scikit-learn>=1.3.0
# xgboost>=2.0.0
# numpy>=1.24.0

# Development dependencies
pytest>=7.4.0
pytest-asyncio>=0.21.0
